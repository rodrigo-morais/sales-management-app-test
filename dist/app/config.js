define(["exports", "module"], function (exports, module) {
    "use strict";

    var config = (function () {
        return {
            url: "https://localhost:8080/"
        };
    })();

    module.exports = config;
});